# 变更日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- 牌局引擎（`src/holdem`）：牌编码、5–7 张求值器、主池/边池计算、手牌状态机、
  确定性牌堆。全部为纯逻辑，不含 IO 与全局随机数。
- PHH（Poker Hand History）牌谱读写，与 PokerKit 双向互认。
- 决策点上下文还原（`history.py`），为统计指标提供统一数据源。
- 位置命名（`positions.py`），2–9 人桌。
- SQLite 落库（`store.py`）：会话、牌局、玩家结果、逐动作上下文；PHH 原文与结构化
  索引并存。
- 蒙特卡洛权益估算（`equity.py`）。
- 占位规则 bot 与五种风格预设：紧凶 / 松凶 / 岩石 / 跟注站 / 疯子（`bots.py`）。
  **非训练用对手**，M1 会整体替换实现。
- 本地服务端（`holdem_server`）：牌桌编排、FastAPI 的 HTTP + WebSocket 接口、
  单文件网页牌桌（无构建步骤，适配手机竖屏）。
- 求值器双实现交叉验证测试；牌谱 PokerKit 外部互认测试；对手底牌不泄露的专项测试。
- 随机自对弈压测脚本 `scripts/soak.py`（含牌谱往返抽验）。
- 项目文档：PRD、ARCHITECTURE、ADR-0001、DEVLOG、CLAUDE.md。

### 新增（M1 进行中）

- 批量自对弈（`batch.py` + `scripts/play_batch.py`，FR-4）：一桌 bot 打上万手，产出
  **带 95% 置信区间的 bb/100** 与 VPIP/PFR/开牌/3bet/WTSD/W$SD。每手重置筹码、按钮逐手
  右移；`shard`/`merge` 支持分片并行，合并口径与顺序跑完全一致。可选落库喂 M2 统计。
- Slumbot 强度基线（`src/holdem_slumbot` + `scripts/play_slumbot.py`，FR-6）：把它的动作串
  回放成 `HandState`，我们的 bot 直接坐上去打；策略接口就是 `callable(HandState) -> Action`。
  **每手用它回的 `winnings` 对账**，协议翻译出错会当场露馅。支持多会话并行与断点存盘。
  首次实测：3000 手、零作废、零对账失败，−24.94 ± 64.24 bb/100（区间跨 0，分不出胜负；
  要测实这个量级约需 2 万手）。
- **翻后求解适配层**（`src/holdem_solver`，M3 起步 / ADR-0005）：局面 → TexasSolver 命令文件
  → 跑二进制 → 策略树，按请求指纹存盘缓存（一个翻牌局面要几分钟，缓存是必需品）。
  纯逻辑与 IO 分离，命令渲染与解析用**真求解器跑出来的样本**做基准测试。
- **在解出来的树上算 EV**（`holdem_solver/evaluate.py`）：求解器只给策略不给 EV，这一层
  固定英雄的两张牌、让对手按解走，算出每条路的 EV 与 EV 损失。验证靠手算对账
  （造一个胜负确定的河牌局面，逐笔核对到分）、口径硬约束（弃牌 EV 恒等于 −已投入）
  与**和求解器可利用度的互证**。跨街（含全下跑马）在真产物上验过。
- **翻牌范围跟踪**（`holdem/range_tracking.py`，FR-11 的第一块）：从打完的牌回推翻牌时
  双方各自的范围、底池与有效筹码——翻后求解的第一个输入。只认表里真有的线路
  （单次加注底池 / 3bet 底池），其余抛 `NotCovered` 并说明原因。
- **一手牌的翻后复盘**（`holdem_solver/review.py`，FR-9）：牌局 → 待打分的决策点 →
  在解出来的树上逐点算 EV 损失。实战尺度会先并进树里，否则那个动作在解里不存在；
  树里没有的尺度、dump 截断的分支**只跳过那一个点并写明原因**，不影响同一手牌的其余点。
- ADR-0005：**翻后后端改用 CPU 版 TexasSolver**——GPU 版 Windows-only、只有 GUI、闭源、
  无任何命令行/API，自动流程里用不了；取代立项时「GPU 优先、CPU 降级」的决定。
- **单挑 200bb 翻前范围表**（`preflop_ranges_hu_200bb.json`，约 23 秒生成）：单挑整树只有
  几十个节点，**直接精确解**，可利用度 0.0008 大盲/手——不走六人桌那套子博弈分解。
  `preflop_policy` 随之**按人数分发**范围表，并加了深度守卫（差一倍以外不查表）。
  接上之后 FR-6 对 Slumbot 的翻前决策**从 0% 变成 97.4% 照解走**；
  实测 3000 手 −79.02 ± 99.35 bb/100（零作废、零对账失败，区间仍跨 0），
  亏损几乎全在大盲位（−142 vs 按钮位 −16）——翻前解放宽了防守，翻后规则兑现不了。
- 求解器支持**任意决策点的分支价值**（`branch_nodes` + `action_advantage`），
  单挑整树解因此能取到防守者那一格的「继续比弃牌好多少」。
- 挤压建模（`SqueezeRisk` / `SqueezeModel`，ADR-0004 修订）：防守者跟注之后按概率被身后
  挤压，被挤压后的打法再切一段子博弈真解。补上了 ADR-0004 的第三条已知简化。
- **漏洞报告**（`holdem_solver/leaks.py` + `scripts/leak_report.py`，FR-10）：把逐点的
  EV 损失按「街 × 翻前角色 × 位置 × 面对什么」归类、加总，**按总漏损排序**（不按平均——
  平均值最大的往往是一年遇不上几次的局面）。报告自曝覆盖率与复盘不了的原因；
  `--plan-only` 不求解，先看这批牌能复盘多少再决定要不要花几小时。

### 修复

- **求解器的下注尺度只有最后一个生效**（`holdem_solver/request.py`）：`set_bet_sizes`
  同一格发多条命令是覆盖而不是追加，多个尺度必须写在同一行。此前树里悄悄只剩一个尺度。
- **下注额被求解器取整到整数单位**：命令文件改用 1/10 大盲为单位（`SolveRequest.scale`），
  粒度从 1bb 变成 0.1bb，解回来的金额除回大盲。此前底池 5.5 打 33% 会变成实际 36.4%。

- 起手牌类与范围（`ranges.py`）：169 个牌类，编号直接对应 13×13 图表布局；
  支持 `TT+`、`A5s-A2s`、`AK`、`99:0.5` 等主流记法与集合运算。
- 精确权益枚举 `exact_equity`：穷举全部公共牌，作为全项目权益数值的基准。
- 预计算的 169×169 翻前权益表（`equity_table.py` + `data/preflop_equity.bin`，约 112 KB），
  含精确可算的共牌权重、对范围与范围对范围的权益。
- 短筹码推/弃纳什求解（`pushfold.py`，CFR+），可利用度低于 1e-4 大盲/手。
- 翻前抽象博弈树（`preflop_tree.py`）：离散化的下注尺度、完整下注序列，可退化成推/弃博弈。
- 翻后收益的压缩模型（`realization.py`）：权益兑现系数 + 归一化份额，终局严格零和。
  参数是**未经校准的假设**，校准计划见 ADR-0003。
- 翻前树的 CFR+ 求解器（`preflop_solver.py`，目前仅单挑）：可利用度自证，并与
  `pushfold` 做退化交叉验证（总频率差 0.1 个百分点内、EV 差 4e-5 大盲/手）。
  完整 100bb 单挑树可利用度 0.0026 大盲/手。
- ADR-0003：深筹码翻前范围的求解路线与翻后压缩模型的取舍、已知限制。
- 两人子博弈（`SubgameConfig`）：可从整桌中途切一段——任意死钱、任意位置、
  「已经开过牌」的局面，是六人桌分解的地基。
- 求解器新增**先验范围**（开牌者的范围由上层定死，不在子博弈里重解）与**分支价值**
  （`BranchValue`，供链式合成在归一化之前合并分支）。
- 六人桌链式求解（`preflop_chain.py`，ADR-0004）：按位置拆成两人子博弈，合成开牌 EV，
  外层 CFR+ 迭代到范围稳定。
- 离线生成的六人桌翻前范围表 + 查询接口（`scripts/build_preflop_ranges.py`、
  `preflop_ranges.py`）：开牌范围、面对开牌的应对、面对再加注的应对，**连生成参数一起存**。
  首版产物 123 KB，8 轮扫描 17.4 分钟；开牌频率 UTG 20.8% / HJ 23.7% / CO 29.6% /
  BTN 41.3% / SB 38.8%，随位置单调变宽。

### 修复

- **「面对再加注」这一格从来没查通过**：策略层去查 `defense(我, 我)`，而表里存的是
  `defense(开牌者, 3bet 者).reraise_reply`。于是这整行局面一直悄悄退回兜底规则，
  **而所有测试都是绿的**——连「72o 查不到」那条都因为查不到而通过。
- **`shared_policy()` 的加载竞态**：先立「试过了」的旗再赋值，多线程并行时后到的线程
  拿到 `None`，那个 bot 整场退回兜底。实测把 FR-6 的照解覆盖率从 97% 打到 49%。
- 批量对局的「开牌率」分子分母口径不一致（跟在跛入者后面的加注也算开牌），
  松手风格能算出 80% 以上。

### 变更

- **翻前范围表重算**（挤压建模，见 ADR-0004 修订）：8 轮 / 23.4 分钟 / 172 KB，
  开牌频率 UTG 14.8% / HJ 18.9% / CO 25.5% / BTN 38.5% / SB 38.8%。冷跟大幅收窄
  （HJ 面对 UTG 19% → 6%），3bet 基本不动，大盲防守（身后无人）不变。
  **方向对但幅度过头**：现在比公开解紧 2–4 个百分点，成因与后续校准计划见 ADR-0004。
  接到 bot 上实测（3000 手）：「照解」风格 VPIP 31.6% → 27.1%（真解约 25%），
  范围表覆盖率 57.7% → 65.8%。六档风格因此集体下移，FR-5 的验收数字要复核。
- `preflop_ranges` 的每一格新增 `squeeze` 字段（该防守者跟注后被挤压的概率）。
- Slumbot 客户端抽成公共模块 `holdem_slumbot.client`，校准脚本与基线脚本共用一套。
- ADR-0004：六人桌翻前范围按位置分解的取舍、合成公式与三条已知简化。
- 翻前查表策略层（`preflop_policy.py`）：从活的牌局认出局面、查解、翻译成引擎动作；
  表覆盖不到的局面明确回 `None`，不拿不适用的解冒充精确解。
- 风格层：`looseness` 沿求解器算出的逐手 EV 排序移动入池频率，`aggression` 调整
  加注与跟注的配比；怂风格在首位由风格层补上跛入。
- 范围表新增逐牌类的开牌 EV 与「继续比弃牌好多少」，供风格层排序。

### 变更

- **`bots.py` 不再是占位实现**：翻前照解出来的范围表打（覆盖不到时退回原规则），
  翻后仍是启发式。类名 `RuleBot` 改为 `Bot`。实测六人自对弈 VPIP/PFR：
  岩石 12.8/8.1、紧凶 22.6/15.9、照解 31.6/19.4、松凶 37.6/28.8、
  跟注站 47.5/2.1、疯子 67.1/60.9。

- 加注额上限由「自己的筹码」改为「自己的筹码与对手能跟到的最大额，取较小者」。
  超出部分本就会原样退回，结果不变；但动作空间更干净，也与主流平台及 PokerKit
  的合法性判定一致。

[未发布]: https://example.invalid/compare
